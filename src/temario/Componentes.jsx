import React from 'react'



// Un componente de react es una funcion que retorna marcado (JSX)
// Tienen una representacion en la UI

// Los componentes pueden ser de dos tipos: FUNCIONALES O DE CLASE


//SEGUN SI TIENEN ESTADO O NO, PUEDEN SER:

// COMPONENTE STATELESS = componente sin estado = no tiene logica, solo recibe datos y muestra datos.

/* const Tarea1 = (props) => {
  return (
    <div>
        <h1>Presentacion</h1>
        <h2>Hola, me llamo {props.nombre} {props.apellido}.</h2>
        <h2>Tengo {props.edad} años.</h2>
        <h3>{props.expectativas}</h3>
    </div>
  )
} */

//COMPONENTE STATEFULL = componente con estado





//sintaxis de Componente Funcional con Arrow Function y Export Default
const Componentes = () => {
  return (
    <div>Componentes</div>
  )
}

export default Componentes